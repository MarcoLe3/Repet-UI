import Button from './ButtonComponent';

export default {
  title: 'Components/Button',
  component: Button,
};

export const Default = {
    args:{
        state: 'default',
        children: 'Send',
    }
}